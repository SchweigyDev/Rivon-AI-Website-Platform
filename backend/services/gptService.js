export async function askGPT(message) {
    console.log("🤖 GPT fallback disabled — local demo mode");

    return `I’m currently running in local-logic demo mode. I can help with Rivon website planning, pricing, lead capture, and getting started.`;
}